import 'package:flutter/material.dart';

class FreeBoard extends StatelessWidget {
  const FreeBoard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('자유 게시판'),),
    );
  }
}
